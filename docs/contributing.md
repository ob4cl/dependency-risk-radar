# Contributing

- Keep the codebase deterministic and local-first.
- Keep parsers pure and predictable.
- Keep network calls behind provider interfaces.
- Add tests for every delivered module.
- Prefer small interfaces over speculative abstractions.

{
  "name": "@drr/github-action",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.cjs",
  "scripts": {
    "build": "corepack pnpm exec esbuild src/index.ts --bundle --platform=node --format=cjs --target=node20 --outfile=dist/index.cjs",
    "test": "vitest run",
    "prepack": "corepack pnpm build"
  },
  "dependencies": {
    "@drr/core": "workspace:*",
    "@drr/shared": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "0.28.0"
  }
}

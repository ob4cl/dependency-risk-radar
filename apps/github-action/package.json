{
  "name": "@drr/github-action",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "corepack pnpm exec esbuild src/index.ts --bundle --platform=node --format=esm --target=node20 --outfile=dist/index.js",
    "test": "vitest run",
    "prepack": "corepack pnpm build"
  },
  "dependencies": {
    "@drr/core": "workspace:*",
    "@drr/shared": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "0.28.0"
  }
}
